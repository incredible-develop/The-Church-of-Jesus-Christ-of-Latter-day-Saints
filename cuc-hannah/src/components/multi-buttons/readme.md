# Multi Buttons

Wrapper for multiple buttons.

## Props

- `justification` - string, a setting for the wrapper justification as a whole.
- `buttons` - array, an array of buttons going inside the wrapper

### Common Props

- `background` - object, see the `background` component for details
- `layout` - object, see the `layout` component for details
